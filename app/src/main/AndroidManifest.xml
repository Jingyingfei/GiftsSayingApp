<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.dllo.giftssayingapp">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".basepackage.VolleyApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_about"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".mainpackage.StartMainActivity" />
        <activity android:name=".mainpackage.WelcomActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".mainpackage.LoginActivity" />
        <activity android:name=".classifypackage.Strategy.EditActivity"></activity>
    </application>

</manifest>